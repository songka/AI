# 飞书自动签核工具开发流程总图

> 本图依据 `完整对话_原档.txt` 和当前项目代码整理。对话编号包含工具结果与日志，不能简单当作独立需求轮数。

## 1. 开发演进

```mermaid
flowchart LR
    A["原始自动签核脚本<br/>只能登录和自动签核"] --> B["CLI 工具箱<br/>查询、详情、签核、拒签<br/>全量操作、dry-run"]
    B --> C["规则引擎<br/>多字段 AND/OR<br/>拒签优先、拒签理由"]
    C --> D["飞书接入<br/>发送、消息回调、互动卡片<br/>群通知、计划任务"]
    D --> E["多用户系统<br/>open_id 隔离<br/>独立账号、规则、设置"]
    E --> F["可视化管理<br/>规则卡、用户组、内容组<br/>设置卡、自定义菜单"]
    F --> G["安全与审计<br/>AI 只建议不执行<br/>二次确认、平台复查"]
    G --> H["完整服务<br/>OAuth 个人统计<br/>Excel 导出、增量提醒"]
```

## 2. 关键设计转变

| 早期方案 | 现场问题 | 当前方案 |
|---|---|---|
| 规则写在代码里 | 每次业务调整都要改程序 | `rules.json` 数据化规则 |
| 白名单/黑名单文本文件 | 多名单难维护、名称不可复用 | 用户组和内容组，规则引用组名 |
| AI 可输出执行动作 | 模拟和疑问句可能被误判 | AI 仅建议明确命令，永不直接签核 |
| 操作提交后立即通知 | 接口成功不等于平台状态成功 | 重新查询验证后才统计和通知 |
| 所有人共用配置 | 账号、规则和数据可能串用 | 以飞书 `open_id` 完整隔离 |
| 自动签核停止即不再查询 | 无法获知新增的待手动内容 | 待手动提醒作为独立只读开关 |
| 所有成功操作统一发群 | 群消息过多且不够灵活 | 手动指定、通知规则、动作规则、默认值四级策略 |

## 3. 当前系统架构

```mermaid
flowchart TB
    U["飞书用户"] -->|私聊、群聊、菜单、卡片| F["飞书开放平台"]
    F -->|事件回调| C["callback_server.py<br/>去重、身份校验、安全路由"]
    S["计划任务<br/>每分钟"] --> T["cli_feishu.py<br/>遍历已登录用户"]

    C --> UM["个人配置<br/>auth / rules / groups / settings"]
    T --> UM
    C --> G["GSC 签核平台"]
    T --> G

    G -->|最新待签数据| R["规则判断"]
    R --> M["待手动 / 需要确认<br/>只私聊新增项目"]
    R --> A["明确人工动作<br/>或自动规则动作"]
    A --> V["提交后重新查询验证"]
    V -->|验证成功| D["个人统计数据库"]
    V --> N["按通知策略发送卡片"]

    O["飞书 OAuth"] --> W["个人统计网页"]
    D --> W
    W --> X["筛选与 Excel 导出"]
```

## 4. 飞书消息处理

```mermaid
flowchart TD
    A["收到飞书事件"] --> B{"回调是否合法？"}
    B -->|否| Z["拒绝"]
    B -->|是| C{"事件是否已处理？"}
    C -->|是| Y["直接返回，不重复回复"]
    C -->|否| D{"事件类型"}

    D -->|菜单| E["打开帮助、查询、规则、组、设置或统计"]
    D -->|卡片| F["解析动作和表单<br/>同步返回 toast 与新卡片"]
    D -->|消息| G{"消息意图"}

    G -->|模拟、测试、预览| H["只读判断，禁止提交和群通知"]
    G -->|查询| I["抓取全部待签<br/>分页卡片完整展示"]
    G -->|明确签核/拒签| J{"是否全量或与规则相反？"}
    J -->|是| K["保存申请单号快照<br/>要求二次确认"]
    J -->|否| L["执行指定项目"]
    K --> L
    G -->|规则、组、设置| M["卡片化管理"]
    G -->|自然聊天或含糊操作| N["AI 回答或提示明确指令<br/>不直接执行动作"]

    L --> O["重新查询平台"]
    O -->|已从待签消失| P["记录统计并按策略通知"]
    O -->|仍在待签| Q["报告失败，不发送成功通知"]
```

## 5. 定时任务

```mermaid
flowchart TD
    A["每分钟启动一次"] --> B["遍历所有用户"]
    B --> C{"自动签核或待手动提醒<br/>是否至少开启一个？"}
    C -->|否| Z["跳过用户"]
    C -->|是| D{"是否在个人时间窗内<br/>且达到执行间隔？"}
    D -->|否| Z
    D -->|是| E["登录并抓取最新待签内容"]
    E --> F["匹配个人规则"]
    F --> G["比较待手动项目快照"]
    G --> H{"是否首次开启？"}
    H -->|是| I["只建立基线"]
    H -->|否且有新增| J["私聊新增待手动项目"]
    H -->|否且无变化| K["不重复提醒"]

    I --> L{"自动签核是否开启？"}
    J --> L
    K --> L
    L -->|否| M["保存快照后结束<br/>绝不执行签核、拒签或等待动作"]
    L -->|是| N["检查等待条件和动作规则"]
    N --> O["提交规则匹配的签核或拒签"]
    O --> P["平台复查"]
    P -->|成功| Q["统计并按策略通知"]
    P -->|失败| R["记录首次异常，相同异常不重复提醒"]
```

## 6. 当前安全底线

- AI 永远不直接执行签核、拒签、全签或全拒。
- 全签和全拒始终按申请单号快照进行二次确认。
- 人工操作与匹配规则相反时必须再次确认。
- 模拟、测试、预览流程不得提交，也不得发送成功群通知。
- 未匹配通知规则时默认不发送群通知。
- 只有平台重新查询验证成功的动作才能进入个人统计。
- OAuth 统计必须在服务端按当前飞书 `open_id` 过滤。
- 待手动提醒可以在自动签核停止时运行，但只能查询和私聊提醒。

## 7. 配套材料

- 主体分区系统架构：`deploy/系统架构分区.md`
- 图片版：`开发流程图谱.svg`
- Skill 开发步骤图片：`Skill开发步骤流程图.svg`
- Skill 开发分析：`如何开发Skill_分析.md`
- 简易报告：`开发经历与使用心得报告.md`
- 原始资料：`完整对话_原档.txt`
- 运行流程：`deploy/流程图.md`
- 综合说明：`deploy/说明书.md`
