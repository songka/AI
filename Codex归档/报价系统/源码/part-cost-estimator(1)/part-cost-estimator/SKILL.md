---
name: part-cost-estimator
description: 根据机械图纸识别零件类别并估算加工成本，使用用户批准的LFAF四类零件统一报价Excel模板输出结果。适用于加工件、钣金件、焊接件、型材组装件的图纸报价、加工费估算、成本核算、厂商报价差异分析和LFAF报价表生成；必须按零件类别选择不同的材料及工序计算方法，保留模板公式、费率、格式、统一汇总、报价对比、基础参数和人工复核记录，并只输出一个最终Excel文件。
---

# Part Cost Estimator

把一份或多份机械图纸转换为一个新的 LFAF 四类零件统一报价工作簿。把所有分类、计算输入、公式结果、来源追溯和人工复核事项保存在最终 `.xlsx` 文件中。

## 最高优先级规则

- 始终使用 `assets/LFAF_四类零件统一报价计算模板_20260807.xlsx`。不要使用旧模板 `lfaF_cost_template.xlsx`，也不要恢复旧版双行报价结构。
- 不要重新设计或重建模板。保留字体、颜色、边框、列宽、行高、合并单元格、Excel 表格、公式、数据验证、汇总公式、基础参数、隐藏设置和打印设置。
- 保留模板中的七张原始工作表，并在最后增加 `图纸附件`。最终工作表顺序必须是：
  1. `使用说明`
  2. `统一汇总`
  3. `加工件`
  4. `钣金件`
  5. `焊接件`
  6. `型材组装件`
  7. `基础参数`
  8. `图纸附件`
- 一张图纸等于一个零件报价行。先识别 `part_type`，再把该行写入对应类别工作表；不要把四类零件强行套用同一种计算方法。
- 每个零件的 `part_type` 必须是 `加工件`、`钣金件`、`焊接件` 或 `型材组装件`。分类不明确时必须创建高等级人工复核事项；不要静默猜测。
- 让模板公式计算材料费、工序费、税金、未税单价、含税单价及总价。不要把公式结果写成固定数字。
- 四类工作表中的 `未税单价` 是 AI 预估未税单价。若提供厂商未税报价，写入 `厂商报价(未税)`；由模板计算 `差价 = AI预估未税 - 厂商报价(未税)` 与 `占比 = 差价 / AI预估未税`。差价为正表示 AI 预估较高，为负表示 AI 预估较低。
- 未填写厂商报价时，`差价` 和 `占比` 必须保持空白，不得把空白厂商报价当作 0。
- `统一汇总`必须同时保留全部 AI 预估总额，以及仅对已填写厂商报价行按数量加权的 `可比AI预估未税`、`厂商报价未税`、`差价` 和 `差价占比`。不要用不可比的全量 AI 金额减去部分厂商报价。
- 税率从 `基础参数` 读取。当前批准值为 13%；不要在生成器中硬编码税率，也不要恢复 17%。
- 保留 `统一汇总`、`基础参数` 和四类计算工作表，不要像旧模板那样删除参考表。
- 清除模板中的四条示例数据，但保留空白公式行、Excel 表格和手工继续增行的能力。
- 不要从人工分析版工作簿复制文本框、自选图形、异常绘图关系或类别页底部手工合计行。报价对比必须使用单元格公式，并集中在 `统一汇总`进行合计。
- 最终只输出一个报价工作簿。不要生成自动保存、恢复、检查点、仅报价或仅附件等额外 `.xlsx` 文件。
- 默认把结果保存在第一份图纸所在资料夹。已有同名文件时自动使用 `_v002`、`_v003` 等版本，不要覆盖历史报价。

## 零件分类规则

按以下顺序分类：

1. 优先读取图纸名称、标题栏、BOM、材料形态、制造说明和用户明确指定的类别。
2. 判断主要成本形成方式：
   - `加工件`：由块料、板料或棒料去除材料，主要使用车、铣、磨、CNC、放电、线切割等工序。
   - `钣金件`：以板材展开、切割、穿孔、折弯、压铆、五金和表处为主。
   - `焊接件`：由多件材料备料、组立和焊接形成，成本受 BOM 重量、焊缝和后处理驱动。
   - `型材组装件`：以标准型材定尺采购、锯切、钻孔、攻牙、连接件和装配为主。
3. 同一图纸混合多种工艺时，以主要材料采购方式和主要成本驱动选择类别，把次要工艺写入该类别的外购、外协、连接件或补充焊接字段。
4. 无法可靠判断时暂按兼容模式处理，但必须标记 `高风险`，要求工程人员确认零件类别后再正式报价。

## 工作流程

1. 收集图纸与报价信息。
   - 支持 PDF、PNG、JPG、截图、CAD 导出图及能生成可读预览的 CAD 文件。
   - CAD 无法直接读取时，要求提供 PDF、图片或截图。

2. 提取并核对图纸。
   - 先读标题栏、材料、数量、技术要求和 BOM，再读各视图及尺寸。
   - 提取来源文件、项目代码、产品图号、材料、数量、成品尺寸、表面处理、关键特征和制造说明。
   - 比对重复尺寸、标题栏与视图、BOM 与图面说明。把冲突、遮挡、单位不明、缺失信息和低置信度识别写入 `review_items`。

3. 确定零件类别并准备对应计算输入。
   - 构造 JSON 前必须读取 [four-category-inputs.md](references/four-category-inputs.md)。
   - 明确写入 `part_type`，并只填写该类别所需的驱动字段。
   - 对无法从图纸直接得到的利用率、速度、工时或辅料费进行保守估算，并把字段加入 `needs_review_fields` 和 `review_items`。
   - AI 估算工时只用于报价，不是最终生产工艺路线。

4. 生成 JSON 提取文件。

```json
{
  "project_name": "optional",
  "customer": "LFAF",
  "quote_by": "optional",
  "quote_date": "YYYY-MM-DD",
  "output_dir": "optional",
  "parts": [
    {
      "source_file": "drawing.pdf",
      "preview_image": "drawing.png",
      "part_type": "钣金件",
      "project_code": "K31",
      "part_no": "UC1002001103",
      "drawing_no": "UC1002001103_F008",
      "material": "SUS304",
      "quantity": 20,
      "vendor_quote_untaxed": 95.00,
      "calculation_inputs": {
        "unfolded_area_m2": 0.06,
        "sheet_thickness_mm": 1.5,
        "material_utilization": 0.78,
        "cut_length_m": 1.8,
        "pierce_count": 14,
        "bend_count": 4
      },
      "needs_review_fields": ["material_utilization", "cutting_speed_m_per_min"],
      "review_items": []
    }
  ]
}
```

5. 运行生成器。

```bash
python scripts/generate_cost_workbook.py --input-json /path/to/parts.json
```

- 省略 `--output` 时保存到第一份图纸旁边。
- 仅在用户明确指定其他位置时传入 `--output /chosen/folder/`。
- 仅在用户提供另一份更新且明确批准的模板时使用 `--template`。

6. 验证最终工作簿。
   - 确认每张图纸只出现在一个类别工作表中。
   - 确认四类行数总和等于图纸数量，`图纸附件`也恰好一张图纸一行。
   - 确认分类与材料形态、BOM 和主要制造方式一致。
   - 确认材料、工序、税金、未税单价、含税单价、数量与总价单元格仍为模板公式。
   - 确认四类工作表均存在 `报价对比`区，厂商报价为空时差价为空；厂商报价存在时差价方向和占比公式正确。
   - 确认 `统一汇总`统计四类工作表，包含可比 AI 预估、厂商报价、差价和差价占比，并按数量加权；`基础参数`税率保持 13%。
   - 确认类别计算页和 `基础参数`没有空白文本框、自选图形或异常绘图关系。
   - 确认所有受影响的产品图号单元格带有 Excel 批注。
   - 确认不确定项在对应行显示 `待复核` 或 `高风险`，并在 `图纸附件`中记录问题、AI 假设、成本影响及具体人工动作。
   - 确认工作簿包含上述八张工作表，且只生成一个最终 `.xlsx`。

## 通用提取规则

- 材料优先取标题栏，其次取技术说明或 BOM。仅在上下文支持时把 `6061`、`AL6061`、`A6061`标准化为 `A6061T6`。
- 数量优先取订单或标题栏；看不清时暂按 1，并标记人工确认。
- 产品图号优先取 `DRAWING No.`；没有时使用零件号或文件名。
- 加工件材料成本使用备料尺寸，不直接使用成品尺寸。长宽通常增加余量并向上取 5 mm，厚度选择不小于成品厚度的标准规格。
- 钣金件优先提取展开面积、板厚、切割长度、穿孔和折弯信息；不要用加工件的长×宽×高材料公式替代。
- 焊接件优先提取 BOM 净重、材料利用率、焊缝长度与类型、组立和后处理；不要只按成品外包络尺寸计算材料。
- 型材组装件优先提取净长度、kg/m、标准料长、排料利用率、切断/钻孔/攻牙数量、连接件和装配时间。

## 人工复核规则

- 始终返回 `review_items`；没有特殊情况时使用空数组。
- 每条复核事项必须包含 `severity`、`category`、`issue`、`ai_assumption`、`cost_impact` 和 `manual_action`。
- 对零件类别错误风险、关键尺寸/材料/数量冲突、严重缺失或不可读信息使用 `high`。
- 对估算利用率、速度、标准工时、备料余量或工艺路线使用 `medium`。
- 对次要追溯或展示不确定性使用 `low`。
- `manual_action`必须具体可执行，例如“确认该图纸属于钣金件还是加工件，并提供展开尺寸”，不要只写“请确认”。

## 输出回应

只返回最终 `.xlsx` 文件链接和简短摘要，说明：

- 共处理多少张图纸以及四类各有多少张。
- 有多少张需要人工确认，是否存在高风险分类或尺寸冲突。
- 已保留四类模板公式、厂商报价对比、统一汇总、基础参数和 13% 税率。
