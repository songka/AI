# 签核工具箱 — 对话开发流程总图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        签核工具箱 项目开发全流程                              │
│                    2026-07-18 ~ 2026-07-25  共 897 轮对话                    │
└─────────────────────────────────────────────────────────────────────────────┘

  第〇阶段：起点                   第一阶段：CLI 工具箱
  ┌──────────┐                    ┌─────────────────────┐
  │ 已有脚本  │  auto_sign.py     │  cli.py 创建          │
  │ 仅自动签  │ ───────────────→  │  login/list/show      │
  │ 无拒签    │  查看本目录        │  approve/reject       │
  └──────────┘                    │  approve-all/reject-all│
                                  │  拒签 radio 自动构造   │
                                  │  ID 范围 1-5 支持     │
                                  │  --dry-run 测试模式   │
                                  └──────────┬──────────┘
                                             │
  第二阶段：凭证 + 离线决策                   │
  ┌─────────────────────────────┐            │
  │  auth.json 分离凭证          │ ←─────────┘
  │  getpass 安全输入             │
  │  rules.json 规则引擎          │
  │  16 操作符 AND/OR 组合        │
  │  export_to_excel()           │
  │  process_excel()             │
  │  fetch / process / run       │
  │  zhconv 简体检测              │
  └──────────┬──────────────────┘
             │
  第三阶段：飞书对接              第四阶段：交互卡片
  ┌─────────────────────┐       ┌─────────────────────┐
  │  feishu.py           │       │  do_query_card()     │
  │  cli_feishu.py       │       │  按钮选签核/拒签      │
  │  callback_server.py  │       │  确认执行             │
  │  群通知卡片           │       │                      │
  │  私发待手动项目        │       │  ⚠ 放弃 → 文本命令   │
  │  card.action.trigger  │       └─────────────────────┘
  └──────────┬───────────┘
             │
  第五阶段：多用户系统              第六阶段：用户设置
  ┌─────────────────────┐       ┌─────────────────────┐
  │  user_manager.py     │       │  settings.json       │
  │  users/ou_xxx/ 目录   │       │  暂停/启动自动签核     │
  │  独立凭证 rules 白名单 │       │  设时间 08:30 21:30   │
  │  登录/登出/状态       │       │  等签核 张三 approve  │
  │  规则管理卡片          │       │  防循环检测           │
  │  白名单飞书管理        │       │  30-60s 随机延时      │
  │  群 @某人 → 个人执行   │       │  sign_events.json    │
  └──────────┬───────────┘       └──────────┬──────────┘
             │                              │
             └──────────┬───────────────────┘
                        │
  第七阶段：AI 集成              部署 & 调试
  ┌─────────────────────┐       ┌─────────────────────┐
  │  ai_rule.py          │       │  RackNerd VPS        │
  │  OpenAI 兼容 API      │       │  宝塔面板             │
  │  DeepSeek 集成        │       │  Python 3.9.7        │
  │  AI 加规则            │       │  Gunicorn + Flask    │
  │  通用 AI 对话          │       │  qh.lfaf.eu.org     │
  │  DO:query/approve    │       │  Cloudflare 拦截修复  │
  │  ai-setup 交互配置    │       │  死循环 bug 修复      │
  └─────────────────────┘       │  import 缺失修复      │
                                │  卡片回调修复          │
                                └─────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
                              最 终 架 构
═══════════════════════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────────────┐
  │                      服务器 qh.lfaf.eu.org                       │
  │                                                                  │
  │  cron 每分钟 ──→ cli_feishu.py send ──→ 遍历 users/             │
  │                      │                   各自规则匹配            │
  │                      │                   自动签核/拒签            │
  │                      │                   群通知+私发             │
  │                      │                                          │
  │  callback_server.py  ←── 飞书事件回调                            │
  │  (gunicorn :7000)    ←── 消息/卡片/按钮                          │
  │        │                                                        │
  │        ├── 私聊命令: 查询 卡片 签核 拒签 全签 全拒               │
  │        ├── 账户管理: 登录 登出 状态                               │
  │        ├── 规则管理: 规则(卡片) 加/删/暂停/AI加                  │
  │        ├── 白名单:   白名单 加/删/内容/合并                      │
  │        ├── 用户设置: 设置 暂停 设时间 等签核 测试自动签核        │
  │        └── AI 对话:  未匹配命令 → AI 理解 → DO:执行             │
  │                                                                  │
  │  users/ou_xxx/  每用户独立:                                      │
  │    auth.json     rules.json     settings.json                    │
  │    whitelist.txt content_whitelist.txt                           │
  │                                                                  │
  │  sign_events.json ← 签核事件（等待检测用）                       │
  └──────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════
                              数 据 流
═══════════════════════════════════════════════════════════════════════════════

  飞书用户                服务器                      GSC 签核平台
  ────────              ─────────                  ──────────────
     │                      │                           │
     │ 私聊/群@消息          │                           │
     │─────────────────────→│                           │
     │                      │ 登录/查询/签核/拒签         │
     │                      │──────────────────────────→│
     │                      │                           │
     │                      │      待签列表/签核结果      │
     │                      │←──────────────────────────│
     │                      │                           │
     │   查询结果/签核确认    │                           │
     │←─────────────────────│                           │
     │                      │                           │
     │              [定时任务] 每分钟                     │
     │                      │────→ 遍历用户              │
     │                      │────→ 规则匹配              │
     │                      │────→ 自动签核              │
     │                      │                           │
     │   群通知卡片 (自动签核) │                           │
     │←─────────────────────│                           │
     │   私发通知 (待手动)    │                           │
     │←─────────────────────│                           │
     │   异常 @用户           │                           │
     │←─────────────────────│                           │


═══════════════════════════════════════════════════════════════════════════════
                           关 键 数 字
═══════════════════════════════════════════════════════════════════════════════

  对话轮数 ───────────── 897 轮
  开发天数 ───────────── 8 天
  Python 文件 ────────── 10 个
  操作符 ────────────── 16 个
  CLI 命令 ──────────── 12 个
  飞书命令 ──────────── 25+ 个
  致命 Bug 修复 ─────── 6 个
  文档文件 ──────────── 7 个
```
