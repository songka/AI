# 修复流断开报错

- 会话 ID：`019f0791-240a-7530-94a8-abab94115846`
- 最后更新：2026-08-12T09:03:03.000Z
- 来源：Codex 本地会话

## 用户

<environment_context>
  <cwd>C:\Users\lfaf-test\Documents\测试</cwd>
  <shell>powershell</shell>
  <current_date>2026-06-27</current_date>
  <timezone>Asia/Shanghai</timezone>
  <filesystem><workspace_roots><root>C:\Users\lfaf-test\Documents\测试</root></workspace_roots><permission_profile type="managed"><file_system type="restricted"><entry access="read"><special>:root</special></entry><entry access="write"><path>C:\Users\lfaf-test\Documents\测试</path></entry><entry access="write"><special>:slash_tmp</special></entry><entry access="write"><special>:tmpdir</special></entry><entry access="read"><path>C:\Users\lfaf-test\Documents\测试\.git</path></entry><entry access="read"><path>C:\Users\lfaf-test\Documents\测试\.agents</path></entry><entry access="read"><path>C:\Users\lfaf-test\Documents\测试\.codex</path></entry></file_system></permission_profile></filesystem>
</environment_context>

## 用户

# Files mentioned by the user:

## codex-clipboard-aed6eee5-5e65-4ad9-8c50-1aa30cff8c4c.png: C:/Users/LFAF-T~1/AppData/Local/Temp/codex-clipboard-aed6eee5-5e65-4ad9-8c50-1aa30cff8c4c.png

## My request for Codex:
我用的自己的api，现在使用你会报错：stream disconnected before completion: stream closed before response.completed
中专那边日志如图
<image name=[Image #1] path="C:\Users\LFAF-T~1\AppData\Local\Temp\codex-clipboard-aed6eee5-5e65-4ad9-8c50-1aa30cff8c4c.png">
</image>

## 助手

我先按 OpenAI/Codex API 兼容性这个方向看：你截图里最关键的信号是 `502` 和 `120s`，这很像中转层在流式响应还没结束时把连接切断了。

## 助手

我会再对照一下 Codex 官方手册里对“自定义提供方/流式响应”的要求，避免只凭经验猜。重点还是先确认：这是 Codex 客户端等 `response.completed`，但你的中转 502 先断了流。

## 助手

本机这个终端里没有直接暴露 `node`，我会用 Codex 自带的运行时再试一次；如果仍取不到手册，我就把这个作为“官方来源未拉取到”的限制说明清楚。

## 助手

我拿到了 Codex 自带的运行时路径，继续查手册。这个步骤只是为了确认配置层面的事实，真正的故障点大概率仍在中转流式转发。

## 助手

手册已更新到本地。我现在只查和你这个报错相关的几处：自定义 API 地址、模型配置、以及常见连接问题。

## 助手

从截图看，问题基本不在 Codex 客户端本身，而在你的中转/代理流式转发链路：

`stream disconnected before completion: stream closed before response.completed`

意思是：Codex 正在等 `/v1/responses` 的 SSE 流里最后的 `response.completed` 事件，但连接提前被关了。

你截图里两个关键信号：

- 失败请求都是 `POST /v1/responses`
- 状态是 `502`
- 用时卡在 `120s`
- 错误是 `Transport error: error decoding respo...`

这非常像中转服务或反代有一个 120 秒超时/最大响应时长，模型还没输出完，中转先断流，于是 Codex 报“stream closed before response.completed”。

优先改这几项：

1. 把中转/反代的流式超时调大  
   如果有 Nginx / Caddy / Cloudflare / 面板代理，重点找：
   - `proxy_read_timeout`
   - `proxy_send_timeout`
   - request timeout
   - upstream timeout
   - stream timeout
   - 最大响应时间

   建议至少 `300s`，更稳一点 `600s`。

2. 确认中转支持 OpenAI `Responses API`，不是只兼容 Chat Completions  
   Codex 现在走的是：

   ```text
   POST /v1/responses
   ```

   并且需要完整转发 SSE 事件，直到：

   ```text
   event: response.completed
   ```

3. 不要让中转“解析再重组”流式响应  
   很多中转会把每个 chunk 当 JSON 解码，结果遇到半包、SSE event、心跳、分片就报 `error decoding response`。最稳的是对 `/v1/responses` 的 `text/event-stream` 原样透传。

4. 关闭 SSE 压缩和缓冲  
   反代层建议：
   - `proxy_buffering off`
   - 不 gzip `text/event-stream`
   - 保留 `Content-Type: text/event-stream`
   - 保留 chunked/流式传输

5. Codex 配置里确认 provider 是 Responses API  
   官方手册里自定义 provider 应类似：

   ```toml
   model_provider = "proxy"
   model = "gpt-5.5"

   [model_providers.proxy]
   name = "OpenAI proxy"
   base_url = "https://你的中转地址/v1"
   env_key = "OPENAI_API_KEY"
   wire_api = "responses"
   stream_idle_timeout_ms = 300000
   stream_max_retries = 10
   ```

   注意这类配置要放在用户级 `~/.codex/config.toml`，项目里的 `.codex/config.toml` 不能覆盖 provider/base_url。

临时绕过可以把 `reasoning effort` 从 `high` 调低，或者换更快模型测试。如果低推理能稳定成功，而 `gpt-5.5/high` 经常 120 秒断，那就几乎坐实是中转 120 秒限制。

## 用户

# Files mentioned by the user:

## config.toml: C:/Users/lfaf-test/.codex/config.toml

## My request for Codex:

## 助手

我看到你提到了 `C:/Users/lfaf-test/.codex/config.toml`，但这条消息里没有具体要求。

你是想让我帮你检查这个配置里和 API 中转/`/v1/responses`/流式断开有关的问题吗？如果是，我可以直接看配置并指出该怎么改。

## 用户

是的

## 助手

好的，我会直接看你的 Codex 配置，重点查 provider、base_url、wire_api、超时和重试这些位置。涉及密钥的内容我只做判断，不会在回复里展开。

## 助手

配置主体我看到了：provider 已经是 `wire_api = "responses"`，这点是对的。现在最扎眼的是你本地配置没有把流式超时显式拉长，但截图里的 `120s` 更像是中转/反代侧的硬超时，不像 Codex 默认超时。

## 助手

我看过了，你这个 [config.toml](C:/Users/lfaf-test/.codex/config.toml:1) 本身大方向没错：

- `model_provider = "myapi"` 指向了自定义 provider
- `base_url = "https://codex.lfaf.eu.org/v1"` 是 `/v1` 结尾，合理
- `wire_api = "responses"` 已经配置了，这点是 Codex 需要的
- TOML 语法解析正常

真正容易触发你截图里 120 秒断流的是这两点：

1. [config.toml 第 5 行](C:/Users/lfaf-test/.codex/config.toml:5) 是 `model_reasoning_effort = "high"`  
   `gpt-5.5/high` 输出慢、推理久，很容易撞到中转的 120 秒超时。

2. 你的 provider 没显式配置流式超时和重试  
   可以在 `[model_providers.myapi]` 下面加：

```toml
request_max_retries = 4
stream_max_retries = 10
stream_idle_timeout_ms = 600000
```

但要注意：这只能让 Codex 这边更能等。你截图里的 `502 + 120s` 更像是 `codex.lfaf.eu.org` 中转/反代先断了，所以最关键还是把中转那边的超时从 `120s` 调到 `300s` 或 `600s`，并确保 `/v1/responses` 的 `text/event-stream` 原样透传到 `response.completed`。

临时验证可以先把第 5 行改成：

```toml
model_reasoning_effort = "medium"
```

如果 `medium` 稳定、`high` 容易 120 秒断，那就基本坐实是中转超时。官方 Codex 配置里自定义 provider 的关键项也是 `base_url`、`env_key`、`wire_api = "responses"` 这一套，你这里已经对上了。
