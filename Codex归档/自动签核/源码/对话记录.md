# 签核工具箱 — 完整对话记录

## 对话 1：初始需求
> 查看本目录，帮忙生成工具，可以登录、检查需要签核的内容、签核、拒签

→ 分析现有 `auto_sign.py`，提出 CLI 工具箱方案
→ 用户确认：先不管判断逻辑，先做 CLI

## 对话 2：CLI 工具箱
> 写出 CLI 工具箱，先不要管判断签核和拒签

→ 创建 `cli.py`（login/list/show/approve/reject/approve-all/reject-all）
→ 修改 `auto_sign.py`：Application 拆为 approve/reject 两套 radio
→ 重构 `submit_approvals()` → `submit_decisions()` 通用提交
→ ID 支持范围写法 1-5

## 对话 3：用户运行 login 成功
> 当前有 1 个待处理项目

## 对话 4：技能文件
> 根据这个写出技能，要把这个 cli 也包进去

→ 创建 `.claude/skills/auto-sign.md`

## 对话 5：中文说明
> 为这个 CLI 加入中文说明

→ 全部帮助文本和输出改为中文，表格对齐，分隔线

## 对话 6：凭证持久化
> 登录需要输入账号密码，输入一次后自动保存到本机，自动登录失败就提示

→ `getpass` 安全输入，`auth.json` 存储
→ `ensure_credentials()` 自动检测，登录失败清除重输

## 对话 7：拒签按钮隐藏
> 没有拒签按钮，是隐藏了，需要给我加入

→ 发现 HTML 只有 approve radio (value=A)
→ 按 ASP.NET 惯例自动构造 reject (同 name, value=R)

## 对话 8：凭证分离到 auth.json
> 规则文件、账户密码在用户执行文件

→ `auth.json` 独立，config.json 无凭证
→ 旧版自动迁移

## 对话 9：分类统计
> 签核内容可以统计显示，分类别

→ `list` 命令加分类统计（类别/单位/申请人，带柱状图）

## 对话 10：定时抓取 + Excel 决策
> 需要一个定时抓取签核内容，生产文件，用户触发上传签核内容
> 自动生成分类，让用户填写需要直接签核、拒签还是需要通知，需要多维条件

→ 全新 v2 架构：`fetch` → Excel → `process`
→ `rules.py` 规则引擎（AND/OR 多维条件）
→ `export_to_excel()` 生成决策表（下拉验证+规则预填）
→ `process_excel()` 批量执行

## 对话 11：目录重组
> CLI 要直接放到 skill，方便 skill 复制给多人使用

→ `.claude/auto-sign/` 技能包目录
→ `deploy/` 部署包

## 对话 12：简体字检测规则
> 料号描述有简体字或者为空直接拒签

→ `has_cn` 操作符 → zhconv 库检测
→ 繁体"软件"误判 → 排除共用字（台、系等）
→ 用户明确：简繁体一样的字不算简体

## 对话 13：content_whitelist 开头匹配
> 开头需要满足 content_whitelist.txt

→ `starts_with_content_wl` 操作符
→ `content_whitelist.txt` 可编辑

## 对话 14：半成品软体特殊规则
> 半成品;軟體的必须是 UOM:ST, Item type:PH，结尾有 R000-R999 版本

→ 拒签规则：条件不满足就拒签
→ `not_equals`、`not_regex` 操作符

## 对话 15：原材料电控外購件规则
> 原材料;電控外購件单位不是 M 或 EA，类别不是 P 的拒签

→ 新增对应拒签规则
→ 用户后续删除这两条签核规则，只保留拒签

## 对话 16：飞书对接
> 现在要再做一个 CLI，用于和飞书的对接

→ `feishu.py` — Webhook/Bot 模式，卡片构建器
→ `cli_feishu.py` — setup/test/send/lookup
→ `飞书对接说明书.md`

## 对话 17：飞书应用机器人配置
> 配置飞书应用机器人

→ App ID/Secret → 权限 → 发布
→ 群成员列表获取 open_id → 宋佳骥 `ou_51fa...`
→ 双通道：群公开摘要 + 私发宋佳骥明细

## 对话 18：回调服务器
> 如何做到回复消息后能自动运行签核？

→ `callback_server.py` — Flask 服务
→ 飞书事件订阅 + 消息命令解析
→ 查询/签核/拒签/全签/全拒

## 对话 19：服务器部署
> 我有公网服务器，安装的宝塔面板

→ 部署到 RackNerd VPS
→ Python 3.9.7 + Gunicorn + Flask
→ qh.lfaf.eu.org 域名 + SSL
→ Cloudflare 拦截 POST body → `request.get_json(force=True)`
→ 端口冲突 → 7000 端口 + 反代
→ Python 2 vs 3 vs 3.9.7 版本问题

## 对话 20：智能通知逻辑
> 有进行签核或拒签的就通知所有人
> 待签核没有发生变化就单独发送给我自己

→ `last_fetch.json` 状态追踪
→ 有变化才通知，不重复打扰

## 对话 21：时间窗口 + 群@触发
> 8:30-21:30 才半小时执行一次，其他时间群里面@就执行
> 2 次执行间隔 > 5 分钟

→ `_in_time_window()` + `_should_debounce()`
→ 群 @某人+@机器人 → 以被@者身份执行

## 对话 22：命令扩展
> 全签 / 全部签核 / 都签 都要支持

→ 多别名匹配

## 对话 23：交互卡片签核
> 需要签核时可以勾选查询的内容后发送

→ 按钮式卡片：每项签核/拒签按钮
→ 选中/取消 + 确认执行
→ **最终放弃**：飞书卡片回调不稳定

## 对话 24：多用户系统
> 以飞书账户做唯一变量，不同用户对应不同的登录账号和密码、不同的规则
> 群里面规则要改为 @对应人 执行对应人的签核

→ `user_manager.py` — users/ou_xxx/ 独立目录
→ 登录/登出/状态命令
→ 规则文本/卡片

## 对话 25：白名单飞书管理
> 每个用户可以在飞书对话查看修改自己的白名单
> 可以合并到规则文件里面

→ 白名单/加白名单/删白名单/合并命令
→ 内联白名单到 rules.json

## 对话 26：规则管理卡片
> 查看规则后也可以以卡片形式选择删除暂停

→ `do_show_rules_card()` — 暂停/删除按钮
→ 暂停规则暂停/恢复，卡片即时刷新

## 对话 27：用户设置
> 设置自动签核的开始结束时间和间隔时间
> 等那个账户签核后自动签核，注意不要出现循环执行

→ 暂停/启动自动签核
→ `设时间 08:30 21:30 30`
→ `等签核 张三 approve`
→ 防循环：A等B → B等A 拒绝 / 链条闭环检测
→ 30-60s 随机延时
→ `sign_events.json` 签核事件记录

## 对话 28：外部 AI
> 我想加入外部 AI
> 我会用 DeepSeek 的 API，用 deepseek-v4-flash 的模型

→ `ai_rule.py` — OpenAI 兼容 API
→ `ai-setup` 交互式配置
→ AI 加规则 → 先外部 AI → 回退内置
→ 通用 AI 对话 → DO:query 等可执行命令

## 对话 29：错误回复帮助
> 飞书对话发送错误词后会回复所有提示词和使用说明

→ 完整帮助信息

## 对话 30：最终审查
> 再检查下整理逻辑和界面，还有所有交底文件，最后一次核查

→ 4 个 Agent 并行审查
→ 发现 5 个致命 bug（import 缺失、死循环）
→ 修复全部 + 同步 deploy/source

## 对话 31：测试自动签核
> 加入测试自动签核，不要真实签核，群里面不要发消息

→ `测试自动签核` 命令 — 只跑规则预览结果

## 对话 32：机器人 ID 自动获取
> 我是在宝塔执行的，他不能自动更新进去吗？

→ `setup.py` — 一键获取 bot_open_id
→ `ai-setup` — 交互式配置 AI

## 对话 33：项目构建记录
> 需要把我们对话建立这个项目的过程整理出来

→ `项目构建记录.md`
→ 本文档
